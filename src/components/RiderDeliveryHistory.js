import React from 'react'

const RiderDeliveryHistory = (props) => {
    const {riderID} = props;
    return (
        <div>
            RiderDelivery History {riderID}
        </div>
    )
}

export default RiderDeliveryHistory
